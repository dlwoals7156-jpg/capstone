{
  "name": "deeplook-capstone",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "frontend:dev": "pnpm --dir frontend dev",
    "frontend:build": "pnpm --dir frontend build",
    "backend:dev": ".venv/bin/python -m uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000"
  }
}
